[package]
name = "lbin-cli"
version = "1.2.0"
edition = "2024"
authors = ["sakura <mail.liminal@pm.me>"]

[dependencies]
anyhow = "1.0.100"
clap = { version = "4.5.53", features = ["derive", "env"] }

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
